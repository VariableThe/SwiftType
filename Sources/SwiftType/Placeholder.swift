// Placeholder or entrypoint
import Foundation
import SwiftTypeCore
import SwiftTypeSystem
import SwiftTypeUI

@main
struct SwiftTypeAppEntry {
    static func main() {
        // Will be replaced with MenuBarExtra App struct once UI is complete
    }
}
